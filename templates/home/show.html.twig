{% extends 'base.html.twig' %}

{% block title %}{{ event.title }}{% endblock %}

{% block body %}
    <div class="container mt-4">
        <a href="{{ path('app_home') }}" class="btn btn-secondary mb-4">&larr; Retour à l'accueil</a>

        <div class="row">
            <div class="col-lg-8 mx-auto">
                <div class="card shadow">
                    <div class="card-header bg-dark text-white p-4">
                        <div class="d-flex justify-content-between align-items-center">
                            <h1 class="h2 mb-0">{{ event.title }}</h1>
                            <span class="badge bg-warning text-dark">{{ event.category.name }}</span>
                        </div>
                    </div>
                    
                    <div class="card-body p-5">
                        <h4 class="text-muted mb-4">
                            📅 Le {{ event.date|date('d F Y') }} à {{ event.date|date('H:i') }}
                        </h4>
                        
                        <hr>
                        
                        <div class="mt-4">
                            <h5 class="mb-3">À propos de cet événement</h5>
                            <p class="lead" style="white-space: pre-line;">
                                {{ event.description }}
                            </p>
                        </div>
                        
                        <div class="mt-5 alert alert-info">
                            Organisé par : <strong>{{ event.author.email }}</strong>
                        </div>

                        {# Bonus : Bouton visible seulement si on est ADMIN #}
                        {% if is_granted('ROLE_ADMIN') %}
                            <div class="d-grid gap-2 mt-4">
                                <a href="{{ path('app_event_edit', {'id': event.id}) }}" class="btn btn-warning">
                                    ⚙️ Modifier (Admin)
                                </a>
                            </div>
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}