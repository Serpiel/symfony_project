{% extends 'base.html.twig' %}

{% block title %}Accueil - SERPIEL SHOWDOWN{% endblock %}

{% block body %}
    <div class="text-center mb-5">
        <h1 class="display-4">Bienvenue sur SERPIEL SHOWDOWN</h1>
        <p class="lead">Découvrez les prochains événements à ne pas manquer.</p>
    </div>

    {# --- DÉBUT : BARRE DE TRI --- #}
    <div class="d-flex justify-content-center mb-5">
        <div class="btn-group" role="group">
            {# Label fixe #}
            <span class="btn btn-dark disabled border-end-0" style="opacity: 1; color: #fff; font-weight: bold;">TRIER PAR :</span>
            
            {# Bouton : Date Croissante (Par défaut) #}
            <a href="{{ path('app_home', {'sort': 'date_asc'}) }}" 
               class="btn {{ currentSort == 'date_asc' ? 'btn-primary' : 'btn-secondary' }}">
               📅 Prochainement
            </a>
            
            {# Bouton : Date Décroissante #}
            <a href="{{ path('app_home', {'sort': 'date_desc'}) }}" 
               class="btn {{ currentSort == 'date_desc' ? 'btn-primary' : 'btn-secondary' }}">
               ⏳ Plus loin
            </a>
            
            {# Bouton : Alphabétique #}
            <a href="{{ path('app_home', {'sort': 'name_asc'}) }}" 
               class="btn {{ currentSort == 'name_asc' ? 'btn-primary' : 'btn-secondary' }}">
               🅰️ A - Z
            </a>
        </div>
    </div>
    {# --- FIN : BARRE DE TRI --- #}

    <div class="row">
        {% for event in events %}
            <div class="col-md-4 mb-4">
                <div class="card h-100 shadow-sm">
                    <div class="card-header bg-primary text-white text-center py-4">
                        <span class="h1">🤘</span>
                    </div>
                    
                    <div class="card-body">
                        {# Badge Catégorie #}
                        <span class="badge bg-secondary mb-2">{{ event.category.name }}</span>
                        
                        <h5 class="card-title">{{ event.title }}</h5>
                        <h6 class="card-subtitle mb-2 text-muted">
                            {{ event.date|date('d/m/Y à H:i') }}
                        </h6>
                        
                        <p class="card-text">
                            {# On coupe la description si elle est trop longue (Filtre Twig) #}
                            {{ event.description|slice(0, 100) }}...
                        </p>
                    </div>
                    
                    <div class="card-footer">
                        <a href="{{ path('app_event_detail', {'id': event.id}) }}" class="btn btn-outline-primary w-100">
                            Voir les détails
                        </a>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="col-12 text-center">
                <p>Aucun événement prévu pour le moment.</p>
            </div>
        {% endfor %}
    </div>
{% endblock %}